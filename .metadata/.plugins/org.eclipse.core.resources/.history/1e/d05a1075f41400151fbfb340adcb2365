package de.uni_hamburg.informatik.swt.se2.kino.werkzeuge.bezahlung;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import de.uni_hamburg.informatik.swt.se2.kino.fachwerte.Geld;

public class BezahlWerkzeug
{
    private BezahlWerkzeugUI _ui;
    
    private Geld zuBezahlenderPreis;
    private Geld rueckzugebenderPreis;

    public BezahlWerkzeug()
    {
        _ui = new BezahlWerkzeugUI();
        registriereUIAktionen();
    }

    private void registriereUIAktionen()
    {
        _ui.getOKButton()
            .addActionListener(new ActionListener()
            {
                @Override
                public void actionPerformed(ActionEvent e)
                {

                }
            });
        _ui.getAbbrechenButton()
            .addActionListener(new ActionListener()
            {
                @Override
                public void actionPerformed(ActionEvent e)
                {

                }
            });
        _ui.getTextField()
            .addActionListener(new ActionListener()
            {
                @Override
                public void actionPerformed(ActionEvent e)
                {
                    
                }
            });
    }
    private Geld bezahlterPreis(){
        return parse(_ui.getTextField().getText());
    }
    
    private Geld parse(String text)
    {
        
    }

    private void berechneRückgabewert(){
        rueckzugebenderPreis  = zuBezahlenderPreis - bezahlterPreis();
    }

}
